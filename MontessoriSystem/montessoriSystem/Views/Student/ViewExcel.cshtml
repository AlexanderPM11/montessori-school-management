@using MontessoriSystem.Core.Application.ViewModels.Student;
@model List<StudentViewModel>

@{
    Layout = null;
}

<div class="container mt-5">
    <div class="card">
        <div class="card-header">
            <h5 class="card-title">Subir Archivo Excel</h5>
        </div>
        <div class="card-body">
            <form id="uploadForm" enctype="multipart/form-data">
                <div class="form-group">
                    <label for="excelFile" class="btn btn-outline-primary">Seleccionar Archivo</label>
                    <input type="file" class="form-control-file d-none" id="excelFile" name="excelFile" accept=".xlsx, .xls" />

                    <!-- Campo para mostrar el nombre del archivo seleccionado -->
                    <input type="text" class="form-control mt-2" id="fileNameDisplay" placeholder="No hay archivo seleccionado" readonly />
                </div>

                <button type="button" id="downloadTemplateExcel" class="btn btn-info mt-3">Descargar Plantilla</button>
                <button type="button" id="uploadBtnExcel" class="btn btn-success mt-3">Subir Archivo</button>
                <button type="button" id="resetBtn" class="btn btn-danger mt-3">Resetear</button>
                <button type="button" id="cancelBtn" class="btn btn-warning mt-3">Cancelar</button>

            </form>
            <div id="result" class="mt-3"></div>
        </div>
    </div>
</div>

<script>
    $(document).ready(function () {


        // Cuando se selecciona un archivo, se muestra su nombre
        $('#excelFile').change(function () {
            var fileName = $('#excelFile')[0].files[0].name;
            $('#fileNameDisplay').val(fileName);
        });

        $('#uploadBtnExcel').click(function () {

            var formData = new FormData();
            var fileInput = $('#excelFile')[0].files[0];

            if (!fileInput) {
                toastr.error('Por favor, seleccione un archivo.');
                return;
            }

            showGlobalLoadingIndicator();

            formData.append('excelFile', fileInput);
            formData.append('IdInstitu', IdInstituStudent);

            $.ajax({
                url: '@Url.Action("CreateStudentExcel", "Student")',
                type: 'POST',
                data: formData,
                contentType: false,
                processData: false,
                success: function (response) {
                    hideGlobalLoadingIndicator();
                    if (response.result) {
                        refresshGrid();
                        confirmDailog.close();                        
                        toastr.success(response.data, 'Correcto!');
                    }else{
                        confirmDailog.close();
                        toastr.error(response.data, 'Error!');
                    }
                    
                },
                error: function (xhr, status, error) {
                    hideGlobalLoadingIndicator();
                    toastr.error('Ocurrio un error. Intente nuevamente!', 'Error!');
                }
            });
        });

        $('#resetBtn').click(function () {
            $('#uploadForm')[0].reset();
            $('#fileNameDisplay').val('No hay archivo seleccionado');
            $('#result').html('');
        });

        $('#cancelBtn').click(function () {
            $('#excelFile').val('');
            $('#fileNameDisplay').val('No hay archivo seleccionado');
            $('#result').html('<div class="alert alert-info">Carga cancelada.</div>');
        });

        $('#downloadTemplateExcel').click(function () {
            window.location.href = '@Url.Action("DownloadTemplateExcel", "Student")';
        });
    });
</script>
